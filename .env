# Valeurs par défaut, commitées. Les secrets vont dans .env.local (ignoré par git), qui l'emporte.
APP_ENV=dev

# Vide : un client scripté répond, sans réseau. Renseigner la vraie clé dans .env.local.
MISTRAL_API_KEY=
