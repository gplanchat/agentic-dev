# Defaults, committed. Secrets go to .env.local (ignored by git), which wins over this file.
APP_ENV=dev

# Empty: a scripted client answers, with no network. Put the real key in .env.local.
MISTRAL_API_KEY=
